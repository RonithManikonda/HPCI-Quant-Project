# Order Book

Minimal in-memory limit order book with a small demo and basic tests.

Build:
```
cmake -S . -B build
cmake --build build
```

Run demo:
```
./build/ob_demo
```

Run tests:
```
./build/ob_tests
```
